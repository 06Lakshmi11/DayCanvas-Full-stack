# MongoDB Atlas connection string (see README for how to get this)
MONGODB_URI=MONGODB_URI=mongodb+srv://livalmiki06_db_user:5TCYPRk26vdv5ZrP@lakshmi.juzlku3.mongodb.net/daycanvas?retryWrites=true&w=majority

# Any long random string — used to sign login tokens
JWT_SECRET=myrandomsecretkey12345abcdefghijklmnop
# Port the backend server runs on
PORT=5000

# Your frontend's URL, so the backend allows requests from it
FRONTEND_URL=http://localhost:5173
